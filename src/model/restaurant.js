class Restaurant {
    
}class Restaurant {
    constructor(name, cuisine, location, rating, menu) {
        this.name = name; // Name of the restaurant
        this.cuisine = cuisine; // Type of cuisine (e.g., Italian, Mexican, etc.)
        this.location = location; // Location of the restaurant (e.g., city, address)
        this.rating = rating; // Rating of the restaurant (out of 5 stars)
        this.menu = menu; // Menu items offered by the restaurant
    }
}